<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.superbaseball.domain.teams.TeamsDao">
	
	<insert id="insert">
		INSERT INTO teams(name, stadiumsId, createdAt) 
		VALUES(#{name}, #{stadiumsId},NOW())
	</insert>

	<select id="findById" resultType="site.metacoding.superbaseball.domain.teams.Teams">
		SELECT * FROM teams WHERE id = #{id}
	</select>
	
	<select id="findAll" resultType="site.metacoding.superbaseball.domain.teams.Teams">
		
	</select>

	<update id="update">
		UPDATE teams SET 
		name = #{name},
		stadiumsId = #{stadiumsId}
		WHERE id = #{id}
	</update>
		
	<delete id="deleteById">
		DELETE FROM teams WHERE id = #{id}
	</delete>
</mapper>